{
  "name": "@sentinelflow/interceptors",
  "version": "0.2.3",
  "description": "Runtime interceptors for AI agent governance — hooks into Claude Code, LangChain, and generic agent frameworks to enforce policies in real-time.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sentinelflow/core": "workspace:*",
    "yaml": "^2.7.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
