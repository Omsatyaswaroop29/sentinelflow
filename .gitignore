# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo

# Generated at build time (copied from repo root so npm's "files" allowlist,
# which is relative to the package root, actually resolves them)
packages/cli/README.md
packages/cli/LICENSE

# Test & coverage
coverage/
.vitest/

# IDE
.vscode/
.idea/
.cursor/
*.swp
*.swo
.DS_Store

# Environment
.env
.env.local
.env.*.local

# SentinelFlow local data (committed: config.yaml; ignored: registry data)
.sentinelflow/agents.json
.sentinelflow/reports.json
.sentinelflow/events.json
.sentinelflow/registry.db

# Turbo
.turbo/

# OS
Thumbs.db

# SentinelFlow runtime data (generated at runtime)
.sentinelflow/events.db
.sentinelflow/events.db-wal
.sentinelflow/events.db-shm
.sentinelflow/events.jsonl
.sentinelflow/events.jsonl.1
.sentinelflow/handler.js
.sentinelflow/cursor-handler.js
.sentinelflow/copilot-handler.js
.sentinelflow/codex-handler.js

# SentinelFlow runtime data
.sentinelflow/events.db
.sentinelflow/events.db-wal
.sentinelflow/events.db-shm
.sentinelflow/events.jsonl
.sentinelflow/events.jsonl.1
.sentinelflow/handler.js
.claude/settings.local.json
results.sarif

# Local handoff notes (keep content in docs/, not this file)
FRIEND-TASKS.md
